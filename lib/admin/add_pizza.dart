import 'package:flutter/material.dart';

class AddPizza extends StatelessWidget {
  const AddPizza({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Add pizza Page'),
    );
  }
}
